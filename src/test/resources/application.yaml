spring:
  profiles:
    active: test
  application:
    name: portfolio


  datasource:
    url: jdbc:mariadb://localhost:3306/test
    username: root
    password: jhlee

mybatis:
  type-aliases-package: com.baro.portfolio.domain
